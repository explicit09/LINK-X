# Core Dependencies
Flask==2.3.2
Flask-Cors==4.0.0
Flask-JWT-Extended==4.5.2
Flask-SQLAlchemy==3.0.5

# Database
psycopg2-binary==2.9.7
SQLAlchemy==1.4.50  # Downgraded to be compatible with langchain
alembic==1.12.0
pgvector==0.2.3

# Authentication & Cloud
firebase-admin==6.2.0
boto3==1.28.57
botocore==1.31.57

# AI/ML Core
openai>=1.6.1,<2.0.0  # Updated to be compatible with langchain-openai
transformers==4.35.2
tiktoken>=0.5.2,<0.6.0  # Updated to be compatible with langchain-openai
numpy==1.24.4
scipy==1.11.3

# Document Processing
PyPDF2==3.0.1
python-docx==0.8.11
python-pptx==0.6.21
unstructured[pdf,docx,pptx]==0.10.19

# Vector Search & RAG
langchain>=0.1.0  # Updated to a version that supports pydantic v2
langchain-community>=0.0.10  # Updated to match langchain version
langchain-openai>=0.0.5  # Keep the updated version
llama_index==0.10.19

# Task Queue
celery[redis]==5.3.4
redis==4.6.0  # Downgraded to be compatible with celery[redis]
hiredis==2.2.3  # Faster Redis client

# Performance & Optimization
ujson==5.8.0  # Faster JSON
orjson==3.9.7  # Even faster JSON
msgpack==1.0.7  # Binary serialization
lz4==4.3.2  # Compression

# API & Streaming
httpx==0.25.0
sse-starlette==1.6.5
aiofiles==23.2.1

# Utilities
python-dotenv==1.0.0
python-multipart==0.0.6
pydantic==2.5.3  # Updated to be compatible with pydantic-settings
pydantic-settings==2.0.3  # Updated to a version that works with pydantic>=2.0.1

# Dependency Injection
dependency-injector==4.41.0

# Monitoring
prometheus-client==0.18.0
python-json-logger==2.0.7

# Additional ML/Data Processing
replicate==0.24.0